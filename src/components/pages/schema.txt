-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.datatable (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  trade_date date NOT NULL,
  symbol text NOT NULL,
  company text NOT NULL,
  open numeric,
  high numeric,
  low numeric,
  close numeric,
  turnover bigint,
  change numeric,
  section text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT datatable_pkey PRIMARY KEY (id),
  CONSTRAINT datatable_date_fk FOREIGN KEY (trade_date) REFERENCES public.market_daily_summary(trade_date)
);
CREATE TABLE public.market_daily_summary (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  trade_date date NOT NULL UNIQUE,
  kse100_prev numeric,
  kse100_close numeric,
  kse100_change numeric,
  kse30_prev numeric,
  kse30_close numeric,
  kse30_change numeric,
  prev_volume bigint,
  curr_volume bigint,
  advances integer,
  declines integer,
  unchanged integer,
  flu_no text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT market_daily_summary_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_trades (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  user_id uuid NOT NULL,
  symbol text NOT NULL,
  trade_type text NOT NULL CHECK (trade_type = ANY (ARRAY['BUY'::text, 'SELL'::text])),
  quantity integer NOT NULL CHECK (quantity > 0),
  price numeric NOT NULL CHECK (price >= 0::numeric),
  trade_date date NOT NULL DEFAULT CURRENT_DATE,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_trades_pkey PRIMARY KEY (id),
  CONSTRAINT user_trades_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.watchlists (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  user_id uuid NOT NULL,
  symbol text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT watchlists_pkey PRIMARY KEY (id),
  CONSTRAINT watchlists_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);

Indexes:
[
  {
    "tablename": "datatable",
    "indexname": "datatable_pkey",
    "indexdef": "CREATE UNIQUE INDEX datatable_pkey ON public.datatable USING btree (id)"
  },
  {
    "tablename": "datatable",
    "indexname": "datatable_symbol_date_uq",
    "indexdef": "CREATE UNIQUE INDEX datatable_symbol_date_uq ON public.datatable USING btree (symbol, trade_date)"
  },
  {
    "tablename": "datatable",
    "indexname": "datatable_trade_date_idx",
    "indexdef": "CREATE INDEX datatable_trade_date_idx ON public.datatable USING btree (trade_date DESC)"
  },
  {
    "tablename": "datatable",
    "indexname": "datatable_symbol_idx",
    "indexdef": "CREATE INDEX datatable_symbol_idx ON public.datatable USING btree (symbol)"
  },
  {
    "tablename": "datatable",
    "indexname": "datatable_section_idx",
    "indexdef": "CREATE INDEX datatable_section_idx ON public.datatable USING btree (section)"
  },
  {
    "tablename": "market_daily_summary",
    "indexname": "market_daily_summary_pkey",
    "indexdef": "CREATE UNIQUE INDEX market_daily_summary_pkey ON public.market_daily_summary USING btree (id)"
  },
  {
    "tablename": "market_daily_summary",
    "indexname": "market_daily_summary_trade_date_key",
    "indexdef": "CREATE UNIQUE INDEX market_daily_summary_trade_date_key ON public.market_daily_summary USING btree (trade_date)"
  },
  {
    "tablename": "user_trades",
    "indexname": "user_trades_pkey",
    "indexdef": "CREATE UNIQUE INDEX user_trades_pkey ON public.user_trades USING btree (id)"
  },
  {
    "tablename": "user_trades",
    "indexname": "user_trades_user_id_idx",
    "indexdef": "CREATE INDEX user_trades_user_id_idx ON public.user_trades USING btree (user_id)"
  },
  {
    "tablename": "watchlists",
    "indexname": "watchlists_pkey",
    "indexdef": "CREATE UNIQUE INDEX watchlists_pkey ON public.watchlists USING btree (id)"
  },
  {
    "tablename": "watchlists",
    "indexname": "watchlists_user_id_symbol_key",
    "indexdef": "CREATE UNIQUE INDEX watchlists_user_id_symbol_key ON public.watchlists USING btree (user_id, symbol)"
  }
]